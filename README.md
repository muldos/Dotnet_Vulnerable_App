This is a super simple .NET console app that is using/depending on an old vulnerable version (12.0.3) of the Newtonsoft JSON library.
I use it to perform basic vulnerability scanning tests with different tools.
